# Environment & secrets
.env
venv/



# Learning/working notes (not part of the final deliverable)
implementation.md

# Python
__pycache__/
*.pyc
*.pyo

# Data (10-K filings are large HTML files — no need to version control them)
data/*
# this file must be tracked bc it is the source of truth for the evaluation
!data/qa_pairs.json

# Chroma vector store (persisted to disk, rebuilt from data)
chroma_db/

# Evaluation outputs (regenerated by src/evaluate.py)
results/

# Jupyter
.ipynb_checkpoints/
